---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import ProjectCard from '../../components/ProjectCard.astro';
import { projectsData } from '../../data/projects';

const { lang } = Astro.props;
const projects = projectsData[lang as 'zh' | 'en' | 'ja'] || projectsData.zh;
---

<BaseLayout title={`项目 — Panda Blog`} description="功能原型与开源实验。" lang={lang}>
  <Header lang={lang} />

  <main class="min-h-screen">
    <div class="section-container pt-10 pb-20">
      <div class="mb-10">
        <h1 class="font-serif text-3xl md:text-4xl mb-2" style="color: var(--text);" data-i18n="proj.title">实验</h1>
        <p class="text-sm" style="color: var(--text-secondary);" data-i18n="proj.desc">功能原型与开源实验。</p>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        {projects.map((project) => (
          <ProjectCard {...project} />
        ))}
      </div>
    </div>
  </main>
  <Footer lang={lang} />
</BaseLayout>
